# The Rider the plugin is compiled and test-run against. Bump to match the IDE you use; the plugin
# has no upper bound, so an older build keeps working in newer Riders.
riderVersion=2025.1.4
sinceBuild=251

org.gradle.jvmargs=-Xmx2g
kotlin.stdlib.default.dependency=false
